# گزارش Torob Product API v3 — پوشاک ترنم

تاریخ: ۱۴۰۵-۰۶-۰۷ / 2026-08-29  
Task: TASK-20260829-001  
مخزن: `D:\soft\Claud\porje\Site B2B`

## خلاصه

فروشگاه تکی به قرارداد رسمی Product API v3 وصل شد. منبع حقیقت PostgreSQL است. یک projection خالص قیمت/موجودی/URL/تصویر/گارانتی را برای API، فید XML و متاتگ PDP می‌سازد. JWT فقط `aud=www.poshaktaranom.ir` را می‌پذیرد. مالک merge/deploy را تأیید کرد.

مستند رسمی پنل پشت کپچا بود؛ قرارداد از آینهٔ GitHub `torob/Torob-Sync` خوانده شد.

## URLهای نهایی

| سطح | URL |
|------|-----|
| Product API v3 | `https://www.poshaktaranom.ir/v1/torob_api/v3/products` |
| فید XML (fallback) | `https://api.poshaktaranom.com/v1/feeds/torob.xml` و alias فروشگاهی `/feeds/torob.xml` |
| Sitemap محصولات تکی | `https://www.poshaktaranom.ir/sitemaps/products.xml` |
| سفارش (جدا) | `https://www.poshaktaranom.ir/api/torob/v1/orders` |
| سلامت ادمین | `GET /v1/torob/health` (JWT ادمین، بدون توکن ترب) |

## دو آدرس جدا در پنل ترب

| صفحه پنل | آدرس |
|------|-----|
| سفارش‌ها / API اختصاصی سفارش | `https://www.poshaktaranom.ir/api/torob/v1/orders` |

تست پنل سفارش مثل محصول GET بدون JWT است و ۲۰۰ با `{success:true,data:[]}` می‌خواهد. همگام‌سازی واقعی همان GET با JWT است.
| همگام‌سازی محصول / Product API v3 | `https://www.poshaktaranom.ir/v1/torob_api/v3/products` |

Audience توکن محصول: `www.poshaktaranom.ir`. آدرس apex بدون www استفاده نشود.

## اقدامات مالک در پنل ترب

1. در تنظیمات Product API آدرس دقیق زیر را وارد کند (HTTPS روی www تکی، بدون apex، بدون اسلش انتهایی اضافه، بدون redirect):  
   `https://www.poshaktaranom.ir/v1/torob_api/v3/products`
2. Audience توکن باید `www.poshaktaranom.ir` باشد.
3. فید XML را فقط به‌عنوان fallback نگه دارد.
4. migration `TorobProductFields1756473600003` با deploy API روی production اجرا می‌شود (ستون‌های additive).
5. محصول `coats00007` تا داشتن تصویر معتبر منتشر نمی‌شود؛ تصویر ساختگی نسازید.

## Migration

فایل: `apps/api/src/database/migrations/20260829-003-torob-product-fields.ts`

- `products.guarantee` varchar(200) nullable
- `products.defaultRetailVariantId` uuid nullable + FK به `product_variants(id)` ON DELETE SET NULL

Rollback: `down()` هر دو ستون و قید را حذف می‌کند. بدون تأیید مالک روی production اجرا نشود.

## تست‌ها

واحد: request parser، projection، تصویر، JWT Ed25519 تستی (منقضی، nbf، aud اشتباه، HS256، none)، صفحه‌بندی ۰/۱/۹۹/۱۰۰/۱۰۱/۲۰۱، واریانت پیش‌فرض، متاتگ PDP از جمله محصول بدون واریانت، migration SQL. API/web `tsc --noEmit` سبز.

Independent Reviewer: PASS WITH CONDITIONS — یافته‌های الزامی رفع شد (`retailUnitStock` مشترک، touch زمان موجودی محصول‌محور).  
Independent Security: PASS WITH CONDITIONS — finding متوسط/بالای باز ندارد؛ شرایط عملیاتی (audience دقیق، بدون کلید تست در production).

Deploy تولید: کد ترب روی `origin/master` است (`2fafc3b`). بیلد تمیز Docker بدون `dist` میزبان، `main` را در `dist/apps/api/src/main.js` می‌گذارد و `@taranom/shared-types` را resolve نمی‌کرد؛ API کرش شد و موقتاً به تصویر `rollback-20260801` برگشت. هات‌فیکس: کامپایل JS پکیج shared-types داخل ایمیج، کپی `packages/shared-types` به runner، و wrapper `dist/main.js`. ورکر omnichannel در این دپلوی بالا نمی‌آید.

## ریسک و rollback

- Audience اشتباه پنل → ۴۰۱
- Host جعلی دیگر audience را عوض نمی‌کند
- غیرفعال‌کردن URL در پنل ترب فوراً خزش API را قطع می‌کند
- فید XML همچنان از همان projection سرو می‌شود

## Skill

`.cursor/skills/torob-integration/` — canonical در repo. نصب کاربر پس از validation با backup.

## فایل‌های اصلی

API: `torob-product-*.ts`, `torob-jwt.ts`, `torob-auth.guard.ts`, feed، product entity/DTO/service، inventory touch، nginx  
وب: PDP meta، sitemap pagination + last-good، ادمین گارانتی/واریانت پیش‌فرض  
اسناد: ADR، این گزارش، WORKLOG، Skill
