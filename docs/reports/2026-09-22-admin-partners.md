# همکاران فروش — ویرایش، حذف، بازطراحی

تاریخ: 2026-09-22

## چه عوض شد

صفحه `/admin/partners` از جدول ساده به فضای کار ادمین تبدیل شد: خلاصه وضعیت، جستجوی نام و موبایل، فیلتر، کارت در موبایل و جدول در دسکتاپ، فرم دعوت/ویرایش، تأیید حذف، و دفتر کمیسیون در پنل کناری.

`PATCH /v1/vendors/:id` حالا شماره ورود را هم عوض می‌کند و همان شماره را روی کاربر همکار می‌نویسد. شماره تکراری ۴۰۹ است.

`DELETE /v1/vendors/:id` فقط همکار بدون کالا، مرسوله، قلم سفارش و ردیف دفتر را حذف می‌کند و کاربر نقش `VENDOR` را هم پاک می‌کند. اگر سابقه باشد، حذف رد می‌شود و صفحه تعلیق را پیشنهاد می‌کند.

## چه عمداً عوض نشد

ورود عمومی همکار ساخته نشد. پنل `/partners` و فاکتور ویترین همان است. شمارش سابقه فقط در پاسخ ادمین است.

## بررسی

- `vendor-policy.spec.ts`: OK
- `apps/api` و `apps/web` با `npx tsc --noEmit`: ۰
- کلیک زنده ادمین انجام نشد چون صفحه پشت ورود مدیر است.
- زنده: VPS `dc639b3`؛ health ۲۰۰؛ هوم عمده ۲۰۰ در ۰٫۴۲ ثانیه؛ هوم تکی ۲۰۰ در ۰٫۲۲ ثانیه؛ `/admin/partners` به ورود مدیر برمی‌گردد.
