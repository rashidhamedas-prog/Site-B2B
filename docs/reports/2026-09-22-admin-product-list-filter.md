# 2026-09-22 — فیلتر پیشرفته فهرست محصولات ادمین

## چه شد

- در `/admin/products` فیلتر پیشرفته اضافه شد: دسته‌بندی (شامل بدون دسته‌بندی)، وضعیت، کالکشن، و فقط موجود.
- معیارها در URL می‌مانند و با کانال و جستجو ترکیب می‌شوند.
- محصولی که دستهٔ اصلی یا عضویت اضافه دارد در همان دسته می‌آید.
- فهرست خالیِ فیلترشده با کاتالوگ خالی فرق دارد.

## چه نشد

- خروجی اکسل هنوز فقط کانال را می‌گیرد.
- فیلتر ویترین و تنظیمات سیستم عوض نشد.

## بررسی

- `npx ts-node --transpile-only src/modules/product/admin-product-list-filter.spec.ts` در `apps/api` موفق بود.
- `npx tsx src/lib/admin-product-workspace.spec.ts` در `apps/web` موفق بود.
- `npx tsc --noEmit` در `apps/api` و `apps/web` بدون خطا بود.
- کلیک زندهٔ ادمین انجام نشد؛ صفحه پشت ورود مدیر است.
