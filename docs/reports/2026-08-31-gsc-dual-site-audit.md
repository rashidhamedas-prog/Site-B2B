# گزارش کنسول جستجو — تکی و عمده (۳۱ مرداد ۱۴۰۵ / 2026-08-31)

جزئیات عددی: `SEO-IMPLEMENTATION-REPORTS/2026-08-31-GSC-DUAL-SITE-AUDIT.md`

## نتیجه

هر دو ملک دامنه (`sc-domain:poshaktaranom.ir` و `sc-domain:poshaktaranom.com`) تأیید شده‌اند، HTTPS سالم است، و ایندکس سایت‌مپ موفق است. گلوگاه رشد ایندکس نیست.

- تکی در ۳ ماه: **۱۰۹ کلیک / ۴٫۴ هزار نمایش / CTR ۲٫۵٪ / رتبه میانگین ۶٫۲** — ۱۷۱ ایندکس، ۱۶۸ خارج.
- عمده در ۳ ماه: **۵۶ کلیک / ۱٫۲۹ هزار نمایش / CTR ۴٫۴٪ / رتبه میانگین ۷** — ۴۹ ایندکس، ۱۱۶ خارج.
- عمده موبایل: **۱۱ URL با LCP بالای ۴ ثانیه** (۰ خوب). تکی هنوز دادهٔ CrUX ندارد.
- تکی Merchant: **۲۷ آیتم بدون image** (علت اصلی: تصویر نسبی در JSON-LD). سیاست مرجوعی/ارسال ساخته نشد چون روزهای واقعی در ادمین ثبت نشده.
- عمده Product snippet نامعتبر: نبود `price` — عمدی است تا قیمت عمده برای مهمان لو نرود.

## کار انجام‌شده در کنسول

- GA4 تکی (`547378194` / ReRetail) به ملک `.ir` وصل شد.
- GA4 `.com` (`547352333`) به ملک عمده وصل شد. استریم هنوز اسم «Retail» دارد — در آنالیتیکس عوض شود.
- سایت‌مپ کانونیکال www تکی submit شد؛ خواندن اول «Couldn't fetch» بود. سایت‌مپ apex (Success / ۸۰ صفحه) دست نخورده ماند.
- Validate Fix روی ریدایرکت / noindex / ۴۰۴ زده نشد.
- ایندکس برای `/account`، منبع ۳۰۱، HTTP، و مسیرهای وردپرس درخواست نشد.

## کد این تسک (روی سرور نمی‌رود تا commit/deploy)

لاغر کردن JSON کارت هوم تکی، ISR برای `/products` بدون فیلتر، ورود پست‌های منتشرشده به `blog.xml`، تغییر انکر فوتر از «خرید عمده»، و مطلق‌کردن تصویر JSON-LD به‌همراه `productGroupID` روی واریانت.

CTA/FAQ هوم تکی در **CMS ادمین** هنوز «حداقل سفارش / خرید عمده» دارد — از `/admin/site-content` عوض شود.

PDP و API محصول امروز مال تسک دیگر است؛ نشت `seoMeta` عمده روی صفحهٔ محصول بعد از استقرار این شاخه ممکن است بماند.
