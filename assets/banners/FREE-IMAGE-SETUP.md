# Image tools for hero banners

## Reality check: fal is not unlimited free
fal.ai gives a small starter credit. Your account returned:

`Exhausted balance` → free credit finished. Top-up is paid.

## Free path (connected now)
**Cursor built-in `GenerateImage`** — no fal balance, no extra API bill.

Use this for:
- 16:9 hero plates
- reference-image guided looks (product / model photos)
- textless backgrounds (Persian copy stays in the website HTML)

## Optional paid later
fal MCP remains configured with `FAL_KEY` (Windows user env + `Key ${env:FAL_KEY}` in Cursor mcp.json) for when you top up billing.
